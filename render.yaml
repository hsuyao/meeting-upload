services:
  - type: web
    name: meeting-upload
    runtime: python
    buildCommand: cd render_app && pip install -r requirements.txt
    startCommand: cd render_app && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    plan: free
    envVars:
      - key: PORT
        value: "5000"
      - key: PYTHON_VERSION
        value: "3.11"